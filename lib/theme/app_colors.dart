import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const bg = Color(0xff14213d);
  static const text = Color(0xffAAAAAA);
  static const primary = Color(0xfffca311);
}
